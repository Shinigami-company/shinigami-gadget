export enum KnowUsableBy {
  NONE = 0,
  VICTIM = 1,
  ATTACKER = 2,
};

export enum FeedbackState {
  NOTHING = 0,
  SENDED = 1,
  RECEIVED = 2
}

export enum rememberTasksType {
  KIRA = 1,
  REVIVE = 2,
};